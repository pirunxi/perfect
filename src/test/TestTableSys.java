package test;

/**
 * Created by HuangQiang on 2017/4/22.
 */
public class TestTableSys {


    public static void main(String[] argv) {
        /*
        PropertyConfigurator.configure("log4j.properties");
        XdbConf xconf = new XdbConf("perfect.gendb.config.xml");
        try {
            Xdb.getInstance().start(xconf, Collections.emptyList());

            new Procedure() {
                @Override
                protected boolean process() {
                    for(int i = 0 ; i < 5; i++)
                        ;//Trace.info("table:{} nextid:{}", xtable.Role1.getTable(), xtable.Role1.nextid());

                    return true;
                }
            }.call();

        } catch (Exception e) {
            e.printStackTrace();
        } finally {
            Xdb.getInstance().stop();
        }
        System.exit(1);
        */
    }
}
